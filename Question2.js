// Question2_*** Use the countries data to find the 10 most spoken languages:
   
   const countries=
   [
     { English: 91 },
     { French: 45 },
     { Arabic: 25 },
     { Spanish: 24 },
     { Russian: 9 },
     { Portuguese: 9 },
     { Dutch: 8 },
     { German: 7 },
     { Chinese: 5 },
     { Swahili: 4 },
     { Serbian: 4 }
   ]












































































   
  //  const setOfLanguages = new Set(languages)
  //  console.log(setOfLanguages)
  //  function mostSpokenLanguages(countries, count) {
  //   // Create an empty object to store the language counts
  //   const languageCounts = {};
  
  //   // Iterate through the countries data and count the number of countries where each language is spoken
  //   for (const country of countries) {
  //     const languages = country.languages;
  //     for (const language of languages) {
  //       if (languageCounts.hasOwnProperty(language)) {
  //         languageCounts[language]++;
  //       } else {
  //         languageCounts[language] = 1;
  //       }
  //     }
  //   }
  
  //   // Convert the languageCounts object into an array of objects with language and count properties
  //   const languageArray = [];
  //   for (const language in languageCounts) {
  //     languageArray.push({ [language]: languageCounts[language] });
  //   }
  
  //   // Sort the languages in descending order of their counts
  //   languageArray.sort((a, b) => Object.values(b)[0] - Object.values(a)[0]);
  
  //   // Return the top 'count' languages
  //   return languageArray.slice(0, count);
  // }
  
  // // Example usage with the given data
   
  // console.log(mostSpokenLanguages(countries, 10));
  
  

 


